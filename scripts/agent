#!/usr/bin/env python3
"""Entry point for the local consent-first CLI."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "apps" / "api"))

from app.cli import main

if __name__ == "__main__":
    main()
